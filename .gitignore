# Secrets — never commit these
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# RAG vector store (created at runtime)
chroma_db/

# Local dev tooling — Claude Code (NEVER commit; must not sync to HF Space)
CLAUDE.md
AUDIT.md
.claude/
skills/
audit-reports/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
