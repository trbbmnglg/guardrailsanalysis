# Pinned to a verified, working set (tested locally on CPython 3.11.15, 2026-05-31).
# Root cause of the prior break: `crewai` was unpinned, so a rebuild pulled a major
# version (1.x) whose Agent(llm=...) contract rejects LangChain ChatOpenAI. Keep these
# pinned; bump deliberately and re-run the verification runbook before deploying.
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.12.5
crewai==1.14.6
crewai-tools==1.14.6
langchain==1.3.2
langchain-core==1.4.0
langchain-openai==1.2.2
langchain-huggingface==1.2.2
openai==2.38.0
litellm==1.86.2
sentence-transformers==5.5.1
huggingface-hub==1.16.1
chromadb==1.1.1
pypdf==6.12.2
python-multipart==0.0.29
pyyaml==6.0.3
slowapi==0.1.9
